# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# uv
.uv/

# PufferLib build artifacts (the vendored source at _pufferlib/ is committed; these are generated)
.venv-shim/
_pufferlib/_C*
_pufferlib/raylib-*/

# RL / training artifacts
wandb/
checkpoints/
logs/
runs/
*.pt
*.ckpt
videos/
*.mp4
*.gif
*.png

# sim-server: the pinned upstream checkout fetch.sh produces (only the overlay is tracked)
sim-server/upstream/

# C build artifacts
*.o
*.so
*.out
deploy/**/*.so
deploy/flash/miniwm

# OS / editor
.DS_Store
.idea/
.vscode/
